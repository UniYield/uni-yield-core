[profile.default]
src = "src"
out = "out"
libs = ["lib"]
script = "script"

[profile.fork]
src = "src"
out = "out"
libs = ["lib"]
script = "script"
# Fork tests load RPC_URL and FORK_BLOCK from .env
# Run: forge test --match-path "test/fork/*.t.sol" --profile fork
